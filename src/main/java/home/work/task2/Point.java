package home.work.task2;

/**
 * Represents a point in a 2D Cartesian coordinate system.
 * This class provides methods to calculate distances between points
 * and to determine the point's position relative to the origin (0,0).
 *
 * Instances of this class are immutable, meaning that once a point
 * is created, its coordinates cannot be changed.
 */
public class Point{
    // Code
}
